# Optional root-level overrides for backend model and upload locations.
# Copy to `.env` only if you need custom paths outside the default repo layout.

PLANTGUARD_MODEL_ROOT=/absolute/path/to/model-root
PLANTGUARD_UPLOADS_DIR=/absolute/path/to/uploads-dir
