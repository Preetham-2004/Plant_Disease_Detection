# Reports

This folder contains historical implementation notes, dataset reports, and audit writeups collected during development.

Some reports reference older file locations from before the repository cleanup. The current source-of-truth layout is:

- `scripts/` for utilities
- `docs/guides/` for onboarding
- `website/` for the application
