# Scripts

- `training/`: model training entry points
- `inference/`: prediction, evaluation, and heatmap utilities
- `verification/`: checks for datasets, labels, and pipeline behavior
- `dataset/`: dataset conversion, merge, and repair helpers
- `monitoring/`: runtime monitoring helpers

Run these scripts from the repository root so existing relative paths continue to work.
